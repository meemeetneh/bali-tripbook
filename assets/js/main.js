const pages=[...document.querySelectorAll('.page')],nav=[...document.querySelectorAll('.bottom button')],drawer=document.getElementById('drawer'),shade=document.getElementById('shade');
const picks=new Map(JSON.parse(localStorage.getItem('tripbook-picks')||'[]'));
function closeDrawer(){drawer.classList.remove('open');shade.classList.remove('open')}
function showPage(name){pages.forEach(p=>p.classList.toggle('active',p.dataset.page===name));nav.forEach(n=>n.classList.toggle('active',n.dataset.go===name));closeDrawer();window.scrollTo({top:0,behavior:'smooth'});if(name==='picks')renderPicks()}
document.querySelectorAll('[data-go]').forEach(el=>el.addEventListener('click',()=>showPage(el.dataset.go)));
document.getElementById('menuBtn').onclick=()=>{drawer.classList.add('open');shade.classList.add('open')};document.getElementById('closeBtn').onclick=closeDrawer;shade.onclick=closeDrawer;
document.querySelectorAll('[data-filter]').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('[data-filter]').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.place-card').forEach(c=>c.style.display=f==='all'||c.dataset.category===f?'':'none')});
document.querySelectorAll('.place-card').forEach(card=>{const btn=card.querySelector('.save'),title=card.querySelector('h3').textContent;if(picks.has(title)){btn.classList.add('saved');btn.textContent='♥'}btn.onclick=()=>{if(picks.has(title)){picks.delete(title);btn.classList.remove('saved');btn.textContent='♡'}else{picks.set(title,{title,category:card.dataset.category,description:card.querySelector('p').textContent});btn.classList.add('saved');btn.textContent='♥'}localStorage.setItem('tripbook-picks',JSON.stringify([...picks.entries()]))}});
function renderPicks(){const wrap=document.getElementById('saved'),empty=document.getElementById('empty');wrap.innerHTML='';empty.style.display=picks.size?'none':'block';picks.forEach(item=>{const a=document.createElement('article');a.className='place-card';a.innerHTML=`<div class="cover ${item.category}-cover">${item.category}</div><div class="place-body"><div><small>Saved place</small><h3>${item.title}</h3><p>${item.description}</p></div></div>`;wrap.appendChild(a)})}
